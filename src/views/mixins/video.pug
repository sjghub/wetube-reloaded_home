mixin video(video)
    div
        h4 
            a(href=`/videos/${video.id}`)=video.title
        ul          
            li ratings: #{video.rating}/5
            li views: #{video.views}
            li comments: #{video.comments}/5
            li #{video.creatAd}