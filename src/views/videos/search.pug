extends ../base.pug
include mixins/video.pug

block content
    form(method="GET")
        input(type="text" placeholder="Search" name="title") 
        input(type="submit" value="Search") 

    each x in videos
        +video(x)
        